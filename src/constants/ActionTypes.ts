const LOGIN = "LOGIN" as const;
const UPDATE_USER = "UPDATE_USER" as const;
const LOGOUT = "LOGOUT" as const;
const UPDATE_SETTINGS = "UPDATE_SETTINGS" as const;
const INIT = "INIT" as const;
const UPDATE_PROFILES = "UPDATE_PROFILES" as const;
const CACHE_SESSION = "CACHE_SESSION" as const;
const SELECT_SESSION = "SELECT_SESSION" as const;
const SELECT_SESSION_DETAIL = "SELECT_SESSION_DETAIL" as const;
export default {
    INIT,
    LOGIN,
    LOGOUT,
    UPDATE_USER,
    CACHE_SESSION,
    SELECT_SESSION,
    SELECT_SESSION_DETAIL,
    UPDATE_SETTINGS,
    UPDATE_PROFILES
};
